import { Routes } from "./enums";

export const TOKEN_COOKIE_NAME = "authToken";

export const Links = [
  { route: Routes.DASHBOARD, name: "Home" },
  { route: Routes.SESSIONS, name: "Sessions" },
];
