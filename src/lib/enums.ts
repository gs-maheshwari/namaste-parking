export enum Routes {
    LOGIN = '/login',
    DASHBOARD = '/dashboard',
    SESSIONS = '/sessions'
}

export enum RevalidateTags {
    SPACES = 'spaces',
    SESSIONS = 'sessions',
}

export enum VehicleType {
    CAR = "CAR",
    MOTOR = "MOTOR"
};